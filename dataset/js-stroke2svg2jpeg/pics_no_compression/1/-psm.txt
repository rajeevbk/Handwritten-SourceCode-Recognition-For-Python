def Fido duplicates Cself, name, domain Nore, poh =None).

woRetn = Nore

for okie in ter (self)
if cookie nome

== neame :

if domain is Nye or cookie p z= path:

if toReturn 1s not Nome:

vise CookieConfhetEnor (There cre, Tr’ % (nome)
toReturn = covkie ualue

if foRetuyn :

Jetwn +oReturm

use FeyError (name,  olomain, pach )
